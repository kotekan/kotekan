[Unit]
Description=kotekan
After=syslog.target network.target

[Service]
Type=forking
ExecStart=/usr/sbin/kotekan -d /var/lib/kotekan -c /etc/kotekan/kotekan_%H.conf -l 3
PIDFile=/var/run/kotekan.pid

[Install]
WantedBy=default.target
