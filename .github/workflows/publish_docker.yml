# If a pull request to the development (or chord) branch was closed and merged,
# then rebuild the Docker image if there were changes to it

name: kotekan-docker-image-merge-build

on:
  pull_request:
    types: [closed]
    branches:
      - develop
      - chord

jobs:
  build-docker-on-merge:
    # Only run if the PR was actually merged (not just closed)
    if: ${{ github.event.pull_request.merged == true }}
    uses: ./.github/workflows/build_push_docker.yml
    with:
      # No build_branch here: let build_push_docker.yml decide tags
      # based on base/head and whether the Dockerfile changed.
      # build_branch: ${{ github.event.pull_request.base.ref }}
