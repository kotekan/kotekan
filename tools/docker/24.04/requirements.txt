# --- Scientific Core (Pinned for Binary Stability) ---
# REASON: Numpy 2.0 broke the C-ABI. We pin 1.26.4 to ensure
# C++ extensions (kotekan/HighFive) link correctly.
numpy==1.26.4

# REASON: Astropy 6.x is the current stable LTS series for Python 3.12.
astropy==6.1.6

# --- Documentation Stack ---
# REASON: Sphinx 8.x often breaks older plugins. 7.4.7 is the mature standard.
sphinx==7.4.7
# REASON: Patched specifically for Python 3.12 compatibility.
breathe==4.36.0
sphinx_rtd_theme==3.0.2
sphinxcontrib-plantuml==0.31

# --- Utilities ---
flask==3.0.3
h5py==3.11.0
hdf5plugin==4.4.0
mpmath==1.3.0
gmpy2>=2.2.0
future==1.0.0
msgpack==1.0.8
pkgconfig==1.5.5
posix_ipc==1.1.1
requests==2.32.3
tabulate==0.9.0
click==8.1.7

# --- Testing ---
# REASON: Pytest 9.0 is too new (Nov 2025). We stick to the 8.3 standard.
pytest==8.3.3
pytest-cpp==2.5.0
pytest-localserver==0.8.1
pytest-timeout==2.3.1
pytest-xdist==3.6.1