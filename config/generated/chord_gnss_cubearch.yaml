# GENERATED by config/gen_chord_gnss_config.py --cube-archive-instance -- DO NOT HAND-EDIT.
# THE BEAM-CUBE ARCHIVER: every sender's completed ~1 s (subband x element)
# windows, landed raw under /mnt/cs00/data/kvand/gnss_cube/raw.
# wire frame 101200 B = 32 PRN x 8 bin x 32 elem; listen 11070; rest 12052
# 900 frames/file = 91.1 MB
# ⚠️ cube_max_prn, cube_max_bins and the live element count must match EVERY
#    sender: together they set frame_size, and bufferRecv closes any
#    connection that disagrees -- it delivers no data rather than bad data,
#    which is safe but says nothing about which end is wrong.
cpu_affinity: &id001
- 19
- 24
- 31
- 57
- 58
- 59
- 60
- 61
- 62
- 63
cube_buf:
  frame_size: 101200
  kotekan_buffer: standard
  metadata_pool: gnss_pool
  num_frames: 512
cube_recv:
  buf: cube_buf
  cpu_affinity:
  - 19
  - 24
  - 31
  - 57
  - 58
  - 59
  drop_frames: true
  kotekan_stage: bufferRecv
  listen_port: 11070
  num_threads: 6
  use_config_tracker: false
cube_write:
  allow_ndarray: true
  base_dir: /mnt/cs00/data/kvand/gnss_cube/raw
  cpu_affinity:
  - 60
  file_ext: raw
  file_name: gnss_cube
  in_buf: cube_buf
  kotekan_stage: rawFileWrite
  num_frames_per_file: 900
  prefix_hostname: false
gnss_pool:
  kotekan_metadata_pool: GnssChanMetadata
  num_metadata_objects: 4096
log_level: info
rest_server:
  cpu_affinity: *id001
  enable_cors: true
  port: 12052
telescope:
  name: ICETelescope
  num_dishes: 1
  num_polarizations: 1
  query_gps: false
  require_gps: false
type: config
